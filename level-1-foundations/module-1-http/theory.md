# HTTP Internals – Theory

This module covers:

- Request/response lifecycle (client → DNS → TCP → server).
- How Java servers accept connections and map them to threads.
- Where latency appears (network, disk, application code).

For a detailed narrative, see `content/level-1/http-deep-dive.md` in the main repository.

