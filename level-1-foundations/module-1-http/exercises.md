# HTTP Internals – Exercises

Draft space for HTTP exercises.

Populate this file with practical tasks (e.g. trace a request through a Java HTTP server, measure latency under load) before publishing the course.

