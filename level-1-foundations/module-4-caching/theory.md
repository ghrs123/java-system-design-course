# Caching Strategies – Theory

Key topics:

- Why caching (latency, load, throughput).
- In-memory vs distributed caches.
- Cache-aside, write-through, write-behind.
- Invalidation, TTL and cache stampede.

For a detailed narrative, see `content/level-1/caching-fundamentals.md` in the main repository.

