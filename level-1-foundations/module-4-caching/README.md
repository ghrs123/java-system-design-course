# Module 4 – Caching Strategies

**Level 1 – Foundations**

Goal: reduce load on databases with effective caching (in-memory vs distributed), invalidation and TTL, avoiding inconsistency and stampedes.

See the original deep-dive in `content/level-1/caching-fundamentals.md` for the full narrative.

