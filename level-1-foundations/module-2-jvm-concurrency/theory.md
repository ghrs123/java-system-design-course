# JVM & Concurrency – Theory

This module covers:

- JVM thread model (threads, stacks, scheduling).
- Java Memory Model basics (visibility, ordering, atomicity).
- Race conditions, deadlocks and common concurrency pitfalls.

For a detailed narrative, see `content/level-1/jvm-concurrency-deep-dive.md` in the main repository.

