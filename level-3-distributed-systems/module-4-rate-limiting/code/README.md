# Rate Limiting & Traffic Shaping – Code

This directory will contain minimal Java examples for the rate limiting module
(e.g. token bucket filter, Redis-based limiter, NGINX/gateway configs as references).

