# Rate Limiting & Traffic Shaping – Theory

Key topics:

- What rate limiting is (requests per period).
- Fixed window, sliding window, token bucket and leaky bucket algorithms.
- Local vs centralized (Redis/gateway) limits in distributed environments.

See `content/level-3/rate-limiting-traffic-shaping.md` for the full deep-dive.

