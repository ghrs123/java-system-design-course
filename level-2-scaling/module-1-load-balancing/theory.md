# Load Balancing – Theory

Key topics:

- Vertical vs horizontal scaling.
- Load balancer roles (NGINX/HAProxy/ALB).
- Algorithms: round-robin, least-connections, IP hash.
- Stateless vs stateful instances, sticky sessions and shared stores.

See `content/level-2/load-balancing-horizontal-scaling.md` for the full deep-dive.

